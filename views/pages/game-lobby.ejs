<%- include('../partials/head', {
  title: gameName + " lobbies",
  tags: [{
      type: "meta",
      attributes: {
        name: "game-name",
        content: gameName
      }
    }, {
      type: "link",
      attributes: {
        rel: "stylesheet",
        href: "/lobby-styles.css"
      }
    }
  ]
}); %>

<h1>
  Lobbies for: <%= gameName %>
</h1>

<button class='create-lobby'>
  Create lobby of <%= gameName %>
</button>

<% for (const id of ids) { %>
<div class="lobby">
  <p><%= id %></p>
  <button id="<%= id %>">Join lobby</button>
</div>
<% } %>

<script src="/game-lobby-bundle.js"></script>

<%- include('../partials/footer'); %></input>