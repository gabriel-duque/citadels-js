<%- include('../partials/head', {
  title: gameName + " lobbies",
  tags: [{
      type: "meta",
      attributes: {
        name: "game-name",
        content: gameName
      }
    }, {
      type: "link",
      attributes: {
        rel: "stylesheet",
        href: "/lobby-styles.css"
      }
    }
  ]
}); %>

<h1>
  Lobby - <%= gameName %>
</h1>

<button class='create-room'>
  Create room of <%= gameName %>
</button>

<% for (const id of ids) { %>
<div class="lobby">
  <p><%= id %></p>
  <button id="<%= id %>">Join room</button>
</div>
<% } %>

<script src="/game-lobby-bundle.js"></script>

<%- include('../partials/footer'); %></input>